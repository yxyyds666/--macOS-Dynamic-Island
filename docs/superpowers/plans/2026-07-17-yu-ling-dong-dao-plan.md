# Yu灵动岛实现计划（历史版本）

> 历史实现计划。当前实现已改为单个 `NotchWindow`、`IslandView` 两翼 expanded 布局，以及通过 `/usr/bin/perl` 加载 `MediaRemoteAdapter.framework` 的 `AdapterMediaService`。请以仓库根目录的 `project.yml`、`build.sh` 和 `docs/BUILD_INSTRUCTIONS.md` 为当前构建依据。

